How to run:

navigate to the file folder in cmd (outside the src) i.e.     
cd C:\Users\andyp\OneDrive\Desktop\cs421hw3

add files outside of src folder for easy use, otherwise enter the absolute path when prompted.

to compile and run:
javac src/* -d bin && java -cp "bin/;sqlite-jdbc-3.43.0.0.jar" Main

